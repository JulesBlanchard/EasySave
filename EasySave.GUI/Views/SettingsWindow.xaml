<Window x:Class="EasySave.GUI.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="250" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Label Content="Log Format:" Width="120"/>
            <ComboBox Width="150" SelectedValue="{Binding LogFormat, Mode=TwoWay}">
                <ComboBoxItem Content="JSON" />
                <ComboBoxItem Content="XML" />
            </ComboBox>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,10">
            <Label Content="Business Software:" Width="120"/>
            <TextBox Width="150" Text="{Binding BusinessSoftwareName, Mode=TwoWay}"/>
        </StackPanel>
        <Button Grid.Row="2" Content="Save" Width="80" HorizontalAlignment="Left" Command="{Binding SaveSettingsCommand}"/>
    </Grid>
</Window>