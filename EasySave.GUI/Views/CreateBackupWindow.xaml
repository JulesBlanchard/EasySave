<Window x:Class="EasySave.GUI.Views.CreateBackupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" Height="300" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Label Grid.Row="0" Grid.Column="0" Content="{Binding BackupNameLabel}" Margin="5"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding BackupName, Mode=TwoWay}" Margin="5"/>

        <Label Grid.Row="1" Grid.Column="0" Content="{Binding SourcePathLabel}" Margin="5"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding SourcePath, Mode=TwoWay}" Margin="5"/>

        <Label Grid.Row="2" Grid.Column="0" Content="{Binding TargetPathLabel}" Margin="5"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding TargetPath, Mode=TwoWay}" Margin="5"/>

        <Label Grid.Row="3" Grid.Column="0" Content="{Binding BackupTypeLabel}" Margin="5"/>
        <ComboBox Grid.Row="3" Grid.Column="1" SelectedValue="{Binding BackupType, Mode=TwoWay}" Margin="5">
            <ComboBoxItem Content="full" />
            <ComboBoxItem Content="diff" />
        </ComboBox>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
            <Button Content="{Binding CreateButtonText}" Command="{Binding CreateBackupCommand}" Width="80" Margin="5"/>
            <Button Content="{Binding CancelButtonText}" Command="{Binding CancelCommand}" Width="80" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
