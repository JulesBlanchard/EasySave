<Window x:Class="EasySave.GUI.Views.EncryptFileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Crypter un fichier" Height="200" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="5">
            <Label Content="Fichier:" Width="80"/>
            <TextBox Text="{Binding FilePath, Mode=TwoWay}" Width="200"/>
            <Button Content="Parcourir" Command="{Binding BrowseFileCommand}" Margin="5,0,0,0"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="5" Grid.Row="1">
            <Label Content="Clé:" Width="80"/>
            <TextBox Text="{Binding Key, Mode=TwoWay}" Width="200"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="5" Grid.Row="2">
            <Button Content="Crypter" Command="{Binding EncryptCommand}" Width="80" Margin="5"/>
            <Button Content="Annuler" Command="{Binding CancelCommand}" Width="80" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>